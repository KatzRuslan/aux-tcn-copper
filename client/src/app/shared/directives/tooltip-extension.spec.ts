import { TooltipExtension } from './tooltip-extension';

describe('TooltipExtension', () => {
    it('should create an instance', () => {
        const directive = new TooltipExtension();
        expect(directive).toBeTruthy();
    });
});
